<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:layout_gravity="right"
    android:gravity="right|center_vertical"
    android:orientation="horizontal">

    <EditText
        android:id="@+id/action_bar_edit_text_search"
        android:layout_width="@dimen/action_bar_edit_text_search_width"
        android:layout_height="wrap_content"
        android:background="@drawable/etsytheme_edit_text_holo_light"
        android:drawableLeft="@drawable/ic_search"
        android:imeOptions="actionSearch"
        android:inputType="textNoSuggestions"
        android:singleLine="true"
        android:textColor="@color/etsy_text_color"
        android:textSize="@dimen/action_bar_edit_text_search_text_size"
        android:visibility="gone" />

    <ImageButton
        android:id="@+id/action_bar_button_search"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="@drawable/action_bar_highlight_selector"
        android:padding="@dimen/action_bar_button_search_padding"
        android:src="@drawable/ic_search" />

</LinearLayout>